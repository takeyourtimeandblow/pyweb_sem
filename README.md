# Flask Task Manager

Веб-приложение для управления задачами с REST API и веб-интерфейсом. Разработано на Flask без использования SQLAlchemy для обеспечения совместимости с Python 3.13.

## ✨ Особенности

- ✅ **Аутентификация пользователей** (регистрация, вход, выход)
- ✅ **Управление задачами** (создание, редактирование, удаление, просмотр)
- ✅ **Фильтрация задач** по статусу и приоритету
- ✅ **Пагинация** для удобного просмотра задач
- ✅ **Роли пользователей** (обычный пользователь / администратор)
- ✅ **REST API** для интеграции с другими приложениями
- ✅ **Документация API** через веб-интерфейс
- ✅ **SQLite база данных** без зависимостей от ORM
- ✅ **Полностью на Python 3.13+**

## 🚀 Быстрый старт

### 1. Клонирование и настройка

```bash
# Клонирование репозитория
git clone https://github.com/takeyourtimeandblow/pyweb_sem
cd flask-task-manager

# Создание виртуального окружения (Windows)
python -m venv venv
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

```

### 2. Запуск приложения

```bash
python run.py
```

### 3. Доступ к приложению

Откройте браузер и перейдите по адресу:
- **Веб-интерфейс:** http://localhost:5000
- **API документация:** http://localhost:5000/api/docs

### 4. Тестовые пользователи

После первого запуска автоматически создается администратор:
- **Логин:** `admin`
- **Пароль:** `admin123`

Вы также можете зарегистрировать новых пользователей через интерфейс.

## 📁 Структура проекта

```
flask-task-manager/
├── app/
│   ├── __init__.py          # Инициализация Flask приложения
│   ├── auth.py              # Модель пользователя и аутентификация
│   ├── database.py          # Работа с SQLite базой данных
│   ├── models.py            # Модель задачи
│   ├── routes.py            # Все маршруты (веб и API)
│   └── templates/           # HTML шаблоны
│       ├── base.html        # Базовый шаблон
│       ├── login.html       # Страница входа
│       ├── register.html    # Страница регистрации
│       ├── tasks.html       # Список задач
│       ├── task_form.html   # Форма создания/редактирования задачи
│       ├── task_detail.html # Детальный просмотр задачи
│       ├── profile.html     # Профиль пользователя
│       ├── api_docs.html    # Документация API
│       └── admin/           # Административные шаблоны
│           ├── users.html   # Управление пользователями
│           └── tasks.html   # Все задачи (администратор)
├── run.py                   # Точка входа приложения
├── requirements.txt         # Зависимости Python
└── README.md               # Документация
```

## 🔧 Конфигурация

Приложение использует следующие настройки по умолчанию (можно изменить в `app/__init__.py`):

- **SECRET_KEY:** `dev-secret-key-change-in-production` (измените в продакшене!)
- **База данных:** SQLite файл `task_manager.db` в корне проекта
- **Пагинация:** 9 задач на странице для пользователей, 12 для администратора

Для продакшена:
1. Установите переменную окружения `SECRET_KEY`
2. Используйте PostgreSQL или MySQL вместо SQLite
3. Настройте HTTPS

## 📊 База данных

Приложение использует SQLite с ручным управлением запросами. Структура базы данных:

### Таблица `users`
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    email TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    is_admin BOOLEAN DEFAULT 0,
    is_active BOOLEAN DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

### Таблица `tasks`
```sql
CREATE TABLE tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT,
    status TEXT DEFAULT 'new',
    priority TEXT DEFAULT 'medium',
    due_date DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    user_id INTEGER NOT NULL,
    FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE
)
```

## 🔌 REST API

Приложение предоставляет следующие API эндпоинты:

### Аутентификация
- `POST /api/auth/login` - Вход пользователя
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/logout` - Выход из системы

### Задачи
- `GET /api/tasks` - Получение списка задач
- `GET /api/task/<id>` - Получение задачи по ID
- `PUT /api/task/<id>` - Обновление задачи
- `DELETE /api/task/<id>` - Удаление задачи

### Пример запроса через cURL
```bash
# Получение задач (после аутентификации через браузер)
curl -X GET http://localhost:5000/api/tasks \
  -H "Cookie: session=<ваш_session_cookie>"

# Создание задачи через API (используя сессию браузера)
curl -X POST http://localhost:5000/api/task \
  -H "Content-Type: application/json" \
  -H "Cookie: session=<ваш_session_cookie>" \
  -d '{"title": "Новая задача", "description": "Описание задачи", "status": "new", "priority": "medium"}'
```

## 👥 Роли пользователей

### Обычный пользователь
- Создание, редактирование и удаление своих задач
- Просмотр только своих задач
- Фильтрация задач по статусу и приоритету
- Доступ к REST API для своих задач

### Администратор
- Все возможности обычного пользователя
- Просмотр всех задач в системе
- Доступ к списку всех пользователей
- Фильтрация задач по пользователям
- Управление пользователями (просмотр статуса)

## 🛠️ Разработка

### Добавление новой функциональности

1. **Новый маршрут:**
   ```python
   @app.route('/new_route')
   @login_required
   def new_route():
       # Ваша логика
       return render_template('template.html')
   ```

2. **Новый метод API:**
   ```python
   @app.route('/api/new_endpoint', methods=['POST'])
   @login_required
   def new_api_endpoint():
       data = request.json
       # Обработка данных
       return jsonify({'result': 'success'})
   ```

3. **Новый шаблон:**
   Создайте HTML файл в папке `templates/` и используйте наследование от `base.html`

### Тестирование

```bash
# Запуск тестов (если есть)
pytest tests/

# Запуск с покрытием
pytest --cov=app tests/
```

## 🚢 Развертывание

### На локальном сервере
```bash
# Производственный режим (Windows)
set FLASK_ENV=production
python run.py

# Или с gunicorn (если установлен)
gunicorn -w 4 -b 0.0.0.0:5000 run:app
```

### На Heroku
```bash
# Создание приложения
heroku create your-app-name

# Установка переменных окружения
heroku config:set SECRET_KEY=your-secret-key

# Деploy
git push heroku main
```

### Docker
```dockerfile
FROM python:3.13-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["python", "run.py"]
```

## 🔒 Безопасность

- Пароли хешируются с использованием SHA256 и соли
- Сессии защищены секретным ключом
- Проверка прав доступа для всех операций
- Защита от CSRF (через Flask-Login)
- SQL инъекции предотвращаются через параметризованные запросы

## 📝 Известные ограничения

1. **SQLite**: Для высоконагруженных проектов рекомендуется PostgreSQL
2. **Файлы**: Нет поддержки загрузки файлов к задачам
3. **Уведомления**: Нет email или push-уведомлений
4. **Поиск**: Базовая фильтрация, нет полнотекстового поиска

## 📄 Лицензия

MIT License. См. файл LICENSE для подробностей.

## 👨‍💻 Автор

Разработано как учебный проект для изучения Flask и веб-разработки.

---

**Примечание:** Это учебный проект. Для продакшена рекомендуется добавить дополнительные меры безопасности, логирование и мониторинг.
